# Frontend can call the API at this URL locally
NEXT_PUBLIC_API_URL=http://localhost:8000

# DB / Cache (used later when you add real backend logic)
DATABASE_URL=postgresql://stem_user:stem_pass@db:5432/stem
REDIS_URL=redis://redis:6379/0
